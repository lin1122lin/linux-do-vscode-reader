# Change Log

## 0.6.1

- 修复已打开页面不会同步“伪装时只显示纯代码”设置的问题。
- 配置页或 `settings.json` 修改纯代码模式后立即同步所有已打开页面。
- 每次切换全局伪装时重新同步纯代码状态，避免保留旧的可读注释样式。

## 0.6.0

- 话题广场和左侧栏新增“未读”与“新话题”登录专属列表。
- 代码伪装新增“只显示纯代码”开关，可完全隐藏话题标题、列表和正文。
- 未读或新话题为空时显示对应空状态。

## 0.5.0

- 话题阅读页新增手动点赞和取消点赞。
- 新增回复编辑器，支持回复整个话题或指定楼层，并默认二次确认。
- 设置页新增互动总开关、回复确认开关和“回复话题”快捷按钮。
- 写操作复用专用 Chrome 登录会话和 Discourse CSRF 校验，不提供自动或批量互动。

## 0.4.0

- 新增“话题广场显示位置”设置：独立页面、左侧栏、两者同时启用。
- 左侧栏模式恢复原生话题树，支持搜索、分类、最新/热门和持续加载。
- 显示位置变更后提供一键重载窗口操作。

## 0.3.4

- 所有用户可见的“摸鱼模式设置”统一更名为“Linux.do 设置”。
- 左侧视图名称简化为“入口”，扩展显示名称改为“Linux.do 阅读器”。

## 0.3.3

- 代码伪装改为全局切换，话题广场和所有已打开的阅读页同步变化。
- 新打开的话题继承当前全局伪装状态。
- 侧栏联动在全局切换时只执行一次，避免多个页面互相抵消。

## 0.3.2

- 用户界面术语统一由“主题”调整为“话题”。
- 独立话题广场新增代码伪装按钮，并支持 `Ctrl+Alt+L` 快捷切换。
- 伪装话题列表使用代码字体、注释前缀和低调的退出按钮。

## 0.3.1

- 修复话题广场没有向扩展发送初始化 `ready` 消息，导致页面永久停在“正在加载”的问题。
- 调整 Webview 消息监听器注册顺序，并增加初始化握手重试。

## 0.3.0

- 请求层重构为独立持久化 Chrome Profile，通过真实浏览器上下文维持 Cloudflare 会话。
- 移除手工复制 Cookie 和 User-Agent 的主流程。
- 话题列表迁移到独立编辑器页面，支持无限滚动、搜索、分类、最新和热门。
- 左侧活动栏改为轻量入口，不再挤占侧栏显示完整帖子列表。
- Cloudflare 需要复验时提供“打开专用 Chrome”操作。

## 0.2.4

- 设置页新增 Cookie 密码输入框和验证保存按钮。
- Cookie 仍加密保存于 SecretStorage，不写入 `settings.json`，当前值不会回显。

## 0.2.3

- 成功请求后自动接收并合并服务器返回的 `Set-Cookie`。
- 轮换后的会话继续加密保存在 VS Code SecretStorage，不再依赖浏览器页面续期。

## 0.2.2

- 设置页新增 User-Agent 编辑框。
- 设置页新增重新设置和清除 Cookie 的快捷按钮。

## 0.2.1

- 点击帖子图片时默认在 VS Code 阅读器内放大预览。
- 支持点击背景、关闭按钮或按 `Esc` 退出图片预览。
- 设置页可关闭内置图片预览。

## 0.2.0

- 回复按照 `reply_to_post_number` 嵌套为楼中楼，最多显示四层缩进。
- 正文支持滚动接近底部自动加载，可在设置页关闭。
- 话题列表支持持续分页、分类筛选和搜索分页。
- 新增代码伪装，默认快捷键为 `Ctrl+Alt+L`，伪装标签名可自定义。
- 新增可视化设置页，可配置标题、图片、布局、侧栏联动和快捷按钮。

## 0.1.1

- 支持 VS Code `http.proxy` 和大小写代理环境变量。
- 网络失败时显示连接超时、拒绝连接或 DNS 失败等具体原因。

## 0.1.0

- 初始版本：话题列表、热门、搜索、阅读器、回复分页和 SecretStorage 会话管理。
